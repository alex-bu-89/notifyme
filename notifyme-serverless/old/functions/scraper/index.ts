import { scraper } from '../handler';

import * as puppeteer from 'puppeteer';
import { getChrome } from '../../lib/chrome';

export function register(consumer: string[]) {

}

export default {
  register,
}
