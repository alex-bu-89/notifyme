{
  "name": "auslaenderbehoerde-termin",
  "version": "0.0.1",
  "description": "Auslaenderbehoerde Termin",
  "main": "",
  "scripts": {
    "start": "npm run selenium:start; npm run start:check",
    "start:check": "node ./src/lib/checkAppointment.js",
    "selenium:start": "selenium-standalone start > /dev/null 2>&1 &",
    "preselenium:start": "selenium-standalone install",
    "poststart": "pkill -f selenium-standalone",
    "lint": "eslint ./src/**/*.js"
  },
  "dependencies": {
    "config": "^1.28.1",
    "selenium-standalone": "^6.12.0",
    "webdriverio": "^4.9.11",
    "winston": "^3.0.0-rc1"
  },
  "devDependencies": {
    "eslint": "^4.13.1",
    "eslint-config-airbnb-base": "^12.1.0",
    "eslint-plugin-import": "^2.8.0"
  }
}
